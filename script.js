function showMsg(msg1+msg2){
    var allMsg = msg1+msg2;
    return allMsg;
}

function lastChar(str){
    var index = 
    str[str.length-1];
    return(index);
}

function random(m){
    var index = 
    str[str.length-1];
    return(index);
}